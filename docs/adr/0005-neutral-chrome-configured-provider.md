# Neutral chrome, configured provider for acquisition

The UI presents a neutral “GACS Checker” (no multi-tenant branding/CMS), while acquisition copy uses a configured provider name and contact CTA. That keeps the product simple and single-tenant in spirit without pretending there is no commercial party behind the leads.
